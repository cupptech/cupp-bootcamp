Queues

    A queue is an ordered collection of items that follows the FIFO principle (First In First Out).
    The addition of new elements in a queue is at the tail and the removal is from the front.
    The newest element added to the queue must wait at the end of the queue.
    
    Priority queue
    Elements are added and removed based on a priority.
